VITE_API_URL=http://localhost:4000
VITE_ALLOW_CUSTOM_AI_BASE_URL=false

# Optional: shows a demo-credentials hint on the login page. Leave unset
# (or "false") in production.
# VITE_SHOW_DEMO_CREDENTIALS=false

# OAuth callbacks land back on the frontend sources page after the backend
# completes the Google/Microsoft connection flow.
